---
import type { DefaultComponentProps } from '@models'
import * as styles from './BackgroundImageParallax.css'
import { Image } from 'astro:assets'
import BackgroundContentContainer from '@components/layout/BackgroundContentContainer.astro'

interface Props extends DefaultComponentProps {
  src:
    | ImageMetadata
    | Promise<{
        default: ImageMetadata
      }>
  alt: string,
}

const { id, classNames, src, alt } = Astro.props
---

<BackgroundContentContainer id={id} class:list={[classNames]} isParallax={true}>
  <Image slot="background" class={styles.parallaxImage} src={src} alt={alt} width="1920" loading="lazy" />
  <slot>
    <div class={styles.defaultSpacer}>
    </div>
  </slot>
</BackgroundContentContainer>