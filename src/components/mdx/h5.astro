---
import { Heading } from '@michemayer/astro-components/components'
import * as styles from './h.css'

const { 'class:list': classList, ...props } = Astro.props
---

<Heading {...props} level="5" as="h5" class:list={[styles.heading, classList]}>
  <slot />
</Heading>
